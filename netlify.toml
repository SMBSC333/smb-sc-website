[build]
  command = "npm run build"
  publish = "public"

[build.environment]
  NODE_VERSION = "20"

# Edge function handles all dynamic routes
# excludedPath ensures static assets + admin are served directly by Netlify CDN
[[edge_functions]]
  path = "/*"
  function = "index"
  excludedPath = ["/static/*", "/admin/*", "/*.html", "/*.ico", "/*.txt", "/*.xml", "/*.json"]
